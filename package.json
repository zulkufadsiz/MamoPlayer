{
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "commitlint": "commitlint --edit",
    "test:packages": "yarn workspace @mamoplayer/core test && yarn workspace @mamoplayer/pro test"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0"
  }
}
